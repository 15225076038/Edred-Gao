用global mapper打开CAD文件，投影格式一定要选择正确
之后导出文件为kml格式，记住不要选中“创建压缩kmz文件”，
从global mapper导出的kml文件默认编码是ISO，无法正确显示中文，有两种方法：
b、用“记事本”打开kml文件，先把文件第一行的encoding从原来的“iso-8859-1”改为“UTF-8”。然后选择“另存为”，在保存对话框里把编码改为“UTF-8”，保存类型选“所有文件”。
我用的是第二种方法。据说a方法生成的kml文件用某些软件打开会出错。